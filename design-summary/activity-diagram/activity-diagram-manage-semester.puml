@startuml
title Manage Semester Activity Diagram (Create & Edit)
skinparam linetype ortho

|Admin|
start
:Navigates to Academic Year Management;
:Selects specific Academic Year;

|System|
:Displays details and list of Semesters;

|Admin|
if (User Action?) then (Create Semester)
    :Click "Add Semester";
    |System|
    :Displays "Create Semester" form;
    |Admin|
    :Enters Semester Details
    (Type, Start Date, End Date);
    :Submits Form;
    
    |System|
    if (Details Valid?) then (Yes)
        :Creates new Semester;
        :Logs the action;
        :Displays success message;
    else (No)
        :Displays error message;
    endif

else (Edit Semester)
    :Selects Semester from list;
    :Click "Edit";
    |System|
    :Displays "Edit Semester" form
    (Pre-filled with current data);
    |Admin|
    :Modifies Semester Details;
    :Submits Form;
    
    |System|
    if (Details Valid?) then (Yes)
        :Updates Semester;
        :Logs the action;
        :Displays success message;
    else (No)
        :Displays error message;
    endif

endif

:Refreshes Semester List;

stop
@enduml
