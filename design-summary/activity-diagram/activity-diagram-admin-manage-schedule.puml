@startuml
title Manage Schedule Activity Diagram
|Admin|
start
:View Master Schedule Manager;
note right
  Displays grid of Teachers vs Periods/Days
end note

:Click Schedule Cell;
note right
  Specific Teacher and Time Slot
end note

|System|
:Display "Edit Schedule" Dialog;
:List Available Courses for Teacher;

|Admin|
if (Action Type?) then (Assign Course)
    :Search/Select Course from List;
else (Remove Assignment)
    :Select "Clear Slot";
endif

|System|
:Attempt Schedule Update;

if (Conflict Detected?) then (Yes)
    :Display Conflict Alert Dialog;
    note right
        Shows details of overlapping schedules
    end note
else (No)
    if (Update Successful?) then (Yes)
        :Update Grid UI;
        :Show Success Toast;
    else (Error)
        :Show Error Toast;
    endif
endif

stop
@enduml
